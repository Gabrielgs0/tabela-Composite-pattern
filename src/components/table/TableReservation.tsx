const TableNextReservation: React.FC<{ nextReservation: string }> = ({ nextReservation }) => (
    <td className="py-2 px-4 border-b">{nextReservation}</td>
  );

export default TableNextReservation
  
  